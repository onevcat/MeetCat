{
  "name": "meetcat",
  "version": "0.1.0",
  "private": true,
  "description": "Auto-join Google Meet meetings with Chrome Extension and Tauri desktop app",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm run app",
    "bootstrap": "bash scripts/bootstrap.sh",
    "app": "bash scripts/app.sh",
    "dev:extension": "pnpm --filter @meetcat/extension dev",
    "dev:tauri": "pnpm --filter @meetcat/core build && pnpm --filter @meetcat/tauri tauri dev",
    "build:tauri": "pnpm --filter @meetcat/core build && pnpm --filter @meetcat/tauri build:tauri",
    "test": "pnpm -r test && pnpm --filter @meetcat/tauri test:rust",
    "test:unit": "pnpm --filter @meetcat/core test",
    "test:coverage": "pnpm -r test:coverage",
    "test:e2e": "pnpm --filter @meetcat/extension test:e2e",
    "clean": "pnpm -r clean",
    "clean:all": "pnpm -r clean && rm -rf node_modules .turbo packages/tauri/src-tauri/target && find packages -name node_modules -type d -prune -exec rm -rf {} +"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.15.4"
}
